# Profile Image

To add your profile photo:

1. Replace `profile.jpg` with your own photo
2. Make sure your photo is named `profile.jpeg` or `profile.jpg` 
3. Or update the image path in `ProfileSidebar.tsx` to match your filename

The image should be a square (recommended 400x400 pixels or larger) for best results.